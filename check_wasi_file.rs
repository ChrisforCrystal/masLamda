fn main() {
    // Check if we can create a WasiFile from a std File
    // let file = std::fs::File::create("test.log").unwrap();
    // let wasi_file = wasmtime_wasi::tokio::File::from_cap_std(cap_std::fs::File::from_std(file));
    println!("Checking WasiFile...");
}
